program LexParseTest;
var
    int x, z, a[5];
    float y, b[2][3];

func

main {
    x = 1;
    y = 2.0;

    if (x <= 3) {
        z = b[1][2];
    } else {
        z = x;
    };

    if (x >= 4) {
        z = x * y;
    } else {
        z = x / y;
    };

    while (x != 5 && y == 6.0) do {
        write x;
    };

    while (x < 0 || y > 4) do {
        write y;
    };
}